---

title: "SAP HANA & Canvas - Exp:2"
author: "Shahid"
published_at: "2013-10-17"
tags: ["Canvas", "database", "HANA", "SAP", "xsjs"]
canonical: "https://community.sap.com/t5/technology-blog-posts-by-sap/sap-hana-amp-canvas-exp-2/ba-p/13027797"
excerpt: "This application uses Canvas, images, and XSJS services for information flow. The same application runs in two browser tabs and exchanges information simultaneously."
---

> **Summary:** This application uses Canvas, images, and XSJS services for information flow. The same application is used in two different tabs, exchanging information simultaneously.

## Introduction

This application uses Canvas, images, and XSJS services for information flow.

The same application is used in two different tabs, exchanging information simultaneously.

**.INDEX.HTML: Code to define the size of the image window and position the squares**

**.VIEW.JS: Code to draw the squares and their movement**

**.CONTROLLER.JS: Calling methods to exchange information about the positions of the squares**

**INDEX.HTML**

```html
<canvas id="myCanvas" width="482" height="482"></canvas>

<script>
var canvas = document.getElementById('myCanvas');
var ctx = canvas.getContext('2d');
var strDataURI = canvas.toDataURL();
var dx = 5;
var dy = 5;
var x = 200;
var y = 5;
var dx2 = 5;
var dy2 = 5;
var x2 = 200;
var y2 = 5;
var WIDTH = 482;
var HEIGHT = 482;
var img = new Image();
var collision = 0;
var counter = 0;
</script>
```

![Screenshot 1](/blogs/blog-22-sap-hana-canvas-exp2/image-1.jpg)

**.VIEW.JS**

Code in `createContent: function(oController)`

```javascript
createContent: function(oController) {
  init();
  window.addEventListener('keydown', doKeyDown, true);

  function rect(x, y, w, h) {
    ctx.beginPath();
    ctx.rect(x, y, w, h);
    ctx.closePath();
    ctx.fill();
  }

  function rect2(x2, y2, w, h) {
    ctx.beginPath();
    ctx.rect(x2, y2, w, h);
    ctx.closePath();
    ctx.fill();
  }

  function clear() {
    ctx.clearRect(0, 0, WIDTH, HEIGHT);
    ctx.drawImage(img, 0, 0);
  }

  function init() {
    img.crossOrigin = '';
    img.src = "http://www.tinyuploads.com/images/cKdYx9.gif";
    setInterval(draw, 10);
    // setInterval(draw2, 1501);
  }

  function doKeyDown(evt) {
    switch (evt.keyCode) {
      case 38:
        var clr = color.getText();
        if (clr == 'blue') {
          if (y - dy > 0) {
            y -= dy;
            clear();
            checkcollision();

            if (collision == 1) {
              y += dy;
              collision = 0;
            } else {
              var CDate = new Date();
              if (counter == 0) {
                oController.MazeMoveSave('blue', x, y, 'Test6', CDate);
                counter = 1;
              } else {
                oController.MazeMoveRead(x, y, 'blue', 'Test6');
                oController.ReadMazeValX('pink', 'Test6');
                oController.ReadMazeValY('pink', 'Test6');
              }
            }
          }
        }
        break;
    }
  }
}
```

**.CONTROLLER.JS**

Methods are used for:

- saving the current location of the square
- reading the location of the other square

```javascript
MazeMoveSave: function(Square, x, y, Passkey, Date) {
  var aUrl = '../../../services/MultiplyTest.xsjs?cmd=insertmaze' + '&Square=' + Square + '&x=' + x + '&y=' + y + '&Passkey=' + Passkey + '&Date=' + Date;
  jQuery.ajax({
    url: aUrl,
    method: 'GET',
    dataType: 'json',
    error: this.onErrorCall
  });
},

MazeMoveRead: function(x, y, Square, Passkey) {
  var aUrl = '../../../services/MultiplyTest.xsjs?cmd=readmaze' + '&x=' + x + '&y=' + y + '&Square=' + Square + '&Passkey=' + Passkey;
  jQuery.ajax({
    url: aUrl,
    method: 'GET',
    dataType: 'json',
    error: this.onErrorCall
  });
},

ReadMazeValX: function(Square, Passkey) {
  var aUrl = '../../../services/MultiplyTest.xsjs?cmd=readmazevalx' + '&Square=' + Square + '&Passkey=' + Passkey;
  jQuery.ajax({
    url: aUrl,
    method: 'GET',
    dataType: 'json',
    success: this.readx,
    error: this.onErrorCall
  });
},
```

**.XSJS**

Below are sample snippets to retrieve data by:

- calling a procedure
- selecting from a table

```javascript
function performReadMazeValX() {
  var body = '';
  var Square = $.request.parameters.get('Square');
  var Passkey = $.request.parameters.get('Passkey');

  try {
    var query = 'CALL "amohas97.session.models::get_maze"(?,?,?,?)';
    $.trace.debug(query);
    var conn = $.db.getConnection();
    var cstmt = conn.prepareCall(query);
    cstmt.setString(1, Square);
    cstmt.setString(2, Passkey);
    cstmt.execute();
    body = cstmt.getDecimal(3);
  } catch (e) {
    $.response.status = $.net.http.INTERNAL_SERVER_ERROR;
    $.response.setBody(e.message);
    return;
  }

  $.response.setBody(body);
  $.response.status = $.net.http.OK;
}

function performReadMazeValY() {
  var body = '';
  var Square = $.request.parameters.get('Square');
  var Passkey = $.request.parameters.get('Passkey');

  try {
    var query = 'select "y" FROM "WRK_SCH"."amohas97.session.data::insertmazemove" WHERE "Square" = ? AND "Passkey" = ?';
    $.trace.debug(query);
    var conn = $.db.getConnection();
    var pstmt = conn.prepareStatement(query);
    pstmt.setString(1, Square);
    pstmt.setString(2, Passkey);
    var rs = pstmt.executeQuery();

    while (rs.next()) {
      body += rs.getDecimal(1);
    }
  } catch (e) {
    // Sample error handling omitted in original post
  }

  $.response.setBody(body);
  $.response.status = $.net.http.OK;
}
```

## Demo

> **Demo:** [Watch on YouTube](http://www.youtube.com/watch?v=2SWd_iNZXRA)

## Key Takeaways

- This post covers SAP HANA & Canvas - Exp:2 with hands-on examples and step-by-step guidance.
- Practical implementation patterns are demonstrated using real SAP tooling.
- The concepts explored here form part of a broader SAP BTP + HANA learning series.

## References

- [Original SAP Community Post](https://community.sap.com/t5/technology-blog-posts-by-sap/sap-hana-amp-canvas-exp-2/ba-p/13027797)
